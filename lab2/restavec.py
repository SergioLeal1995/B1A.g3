# coding=utf-8
import math
import cmath
import numpy as np
def restavec(x,y):	#definicion de la función suma
	print xx-yy

#inicio del programa principal
x = input("Ingrese el valor de x: ")
y = input("Ingrese el valor de y: ")
xx = np.array(x)
yy = np.array(y)

restavec(x,y)	#invocación de la función
